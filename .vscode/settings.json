// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        "**/__pycache__": true,
        "**/.venv": true,
        "**/.vscode": true
    }
}